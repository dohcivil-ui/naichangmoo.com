node_modules/
.next/
out/
coverage/
*.tsbuildinfo
.env
.env.*
!.env.example
*.log
pnpm-debug.log*
.DS_Store
# Runtime working directories at the repository root. Anchored with a leading slash:
# an unanchored data/ also matched src/data, which holds source-controlled reference tables.
/uploads/
/exports/
/tmp/
/data/

# Reference knowledge base: licensed textbooks, official price/labour circulars and
# working screenshots. Read locally, never published with the source.
km/

# Unrelated design reference downloaded ad hoc. Move to km/ if it is worth keeping.
skill/
