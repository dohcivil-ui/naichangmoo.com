node_modules/
.next/
out/
coverage/
*.tsbuildinfo
.env
.env.*
!.env.example
*.log
pnpm-debug.log*
.DS_Store
# Runtime working directories at the repository root. Anchored with a leading slash:
# an unanchored data/ also matched src/data, which holds source-controlled reference tables.
/uploads/
/exports/
/tmp/
/data/

# Reference knowledge base: licensed textbooks, official price/labour circulars and
# working screenshots. Read locally, never published with the source.
km/

# Unrelated design reference downloaded ad hoc. Move to km/ if it is worth keeping.
skill/

# skill ของคนเขียนโค้ดที่ติดตั้งไว้ใช้เอง ไม่ใช่ส่วนหนึ่งของแอป
# โปรเจกต์ปักไว้หกตัวเท่านั้น สามตัวที่ AGENTS.md ข้อ 1 บังคับให้เรียก และสามตัวที่เราเขียนเอง
# ตัวอื่นติดตั้งเพิ่มได้ตามใจ แต่ไม่เข้ารีโป เพราะไม่ได้ทำให้แอปดีขึ้นและทำให้มองไม่ออกว่าอะไรคือของเรา
.claude/skills/*
!.claude/skills/app-shell/
!.claude/skills/thai-docx/
!.claude/skills/drawing-geometry/
!.claude/skills/domain-modeling/
!.claude/skills/grill-with-docs/
!.claude/skills/grilling/
.agents/skills/*
!.agents/skills/app-shell/
!.agents/skills/thai-docx/
!.agents/skills/drawing-geometry/
!.agents/skills/domain-modeling/
!.agents/skills/grill-with-docs/
!.agents/skills/grilling/
