# กติกาของเรพอนี้ — โหลดเข้า context ทุก session

**ไฟล์นี้มีหน้าที่เดียว คือพาไปอ่านของที่เหลือ** ห้ามคัดกฎมาไว้ที่นี่ ทุกบรรทัดในไฟล์นี้
กินที่ใน context ทุกรอบแม้ในงานที่ไม่เกี่ยวกัน · กติกาจริงอยู่ที่ `AGENTS.md`

## อ่านตามลำดับนี้ก่อนแตะซอร์ส

1. **`AGENTS.md`** — บทบาท กติกาการทำงาน ความปลอดภัย และกฎการ commit
2. **ไม้ต่อล่าสุด** — เปิด `docs/handoff/index.json` แล้วอ่านไฟล์ที่ `path` ของ**รายการแรก**
3. **`.claude/memory/MEMORY.md`** — สารบัญความจำ เปิดเฉพาะไฟล์ที่เกี่ยวกับงานตรงหน้า

**ทำไมต้องผ่าน `index.json` ไม่ใช่ระบุชื่อไฟล์ไว้ตรง ๆ** ชื่อไฟล์จะเก่าทุกรอบที่มีไม้ต่อใหม่
แล้วบรรทัดนี้จะพาคนไปอ่านของเก่าโดยไม่มีอะไรบอก · ส่วน**รายการแรกของ `index.json`
มีด่านเฝ้าอยู่แล้วว่าต้องเป็นฉบับใหม่ที่สุดเสมอ** ที่ `src/handoff-index-fence.test.ts`
ตัวชี้ที่มีเทสต์ค้ำ จึงเชื่อได้มากกว่าชื่อที่พิมพ์ไว้

## ทำไมไฟล์นี้ถึงต้องมี

`~/.claude/CLAUDE.md` สั่งให้อ่าน `AGENTS.md` และ `AGENTS.md` สั่งให้อ่านไม้ต่อล่าสุด
**แต่ทั้งสองขั้นเป็นคำสั่งให้ทำตาม ไม่ใช่กลไก** และ `AGENTS.md` ไม่ได้ถูกโหลดเข้า context
เอง ต้องมีใครสั่งให้เปิด · ผลคือเซสชันที่เปิดขึ้นมาโดยไม่มีคำสั่งเปิดแบบละเอียด
จะเริ่มงานโดยไม่รู้เลยว่ามีไม้ต่ออยู่ ซึ่งเกิดขึ้นจริงแล้วเมื่อ 2026-09-07

ไฟล์นี้ถูกโหลดอัตโนมัติ จึงเป็นที่เดียวที่ปิดรูนั้นได้โดยไม่ต้องพึ่งความจำของใคร

## สองข้อที่ต้องรู้ก่อนพิมพ์คำสั่ง git

- **`image/` เป็นของเจ้าของงาน ห้ามแตะ ห้าม commit** มันขึ้นเป็น `??` ใน `git status`
  ตลอดเวลาโดยตั้งใจ · **ห้ามใช้ `git add -A` หรือ `git add .`** ให้ระบุไฟล์ทีละตัวเสมอ
- **`pnpm build` เขียนทับ `next-env.d.ts`** ให้ชี้ path ของ build แทน dev ·
  คืนด้วย `git checkout -- next-env.d.ts` ทุกครั้งหลัง build ไม่งั้นมันจะติดไปกับ commit
