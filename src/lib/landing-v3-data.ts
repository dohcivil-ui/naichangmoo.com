import type { VisualAssetKey } from "@/lib/visual-assets";

/**
 * ค่าตัวอย่างของหน้าแรกรุ่นสาม ที่ยังไม่มีเจ้าของจริงในระบบ
 *
 * **ทำไมต้องมีไฟล์นี้** หน้าแรกรุ่นสามมีของอยู่สามชนิดที่ยังไม่มีที่อยู่จริง — ราคารายแอป
 * หัวข้อบทความ และเบอร์โทร ถ้าปล่อยให้กระจายอยู่ตามคอมโพเนนต์ วันที่ค่าจริงมาถึงจะต้อง
 * ไล่เก็บทีละจุดและจะเหลือค้างไว้จุดหนึ่งเสมอ ทุกค่าที่ยังไม่จริงจึงมาจบที่ไฟล์เดียวนี้
 *
 * **กติกาของไฟล์นี้** ทุกค่าในนี้เป็นค่าที่รอเจ้าของงานยืนยัน ห้ามเพิ่มค่าที่ระบบมีเจ้าของแล้ว
 * เข้ามา เพราะจะกลายเป็นสำเนาที่แข่งกับต้นทาง สามอย่างที่ **ไม่อยู่** ในไฟล์นี้โดยตั้งใจ
 *
 * - **ป้ายสิทธิ์ ราคาแพ็กเกจ และถ้อยคำความพร้อม** มาจากทะเบียนแอปเท่านั้น (ADR 0015, 0018)
 *   ไฟล์นี้เก็บได้แค่ slug ให้คอมโพเนนต์เอาไปถามทะเบียน
 * - **ชื่อและที่อยู่ผู้ประกอบการ** อยู่ที่ `businessOperator` ใน `@/lib/business-identity`
 *   ซึ่งเป็นนิติบุคคลที่รับเงินจริง แก้ที่เดียวแล้วท้ายเว็บทุกหน้าเปลี่ยนตาม
 * - **LINE, Facebook และอีเมล** อยู่ในตารางที่ผู้ดูแลกรอกจากหลังบ้าน อ่านด้วย
 *   `readPublishedChannels()` เจ้าของงานเคาะไว้ 2026-08-28 ว่าช่องที่ยังไม่กรอกให้ซ่อน
 *   ไม่โชว์ค่าปลอมให้ลูกค้าเห็นแม้แต่วินาทีเดียว — การพิมพ์ลิงก์ตายไว้ที่นี่จะล้มคำวินิจฉัยนั้น
 */

/**
 * **วันสิ้นสุดโปรโมชั่น เป็นวันจริงที่คงที่ ไม่ใช่ระยะเวลาจากตอนนี้**
 *
 * ผืนออกแบบเขียนสูตรไว้ว่า `floor(now/วัน)*วัน + 5*วัน` ซึ่งแปลว่า "อีกห้าวันจากตอนนี้"
 * นาฬิกาแบบนั้นรีเซ็ตทุกครั้งที่มีคนโหลดหน้า จึงเหลือห้าวันตลอดกาลและไม่มีวันหมด ·
 * โปรโมชั่นที่ไม่มีวันจบแต่แสดงว่ากำลังจะจบ เป็นคำแถลงเท็จเรื่องเวลา ตระกูลเดียวกับ
 * เปอร์เซ็นต์ความคืบหน้าที่ ADR 0015 ปฏิเสธไปแล้ว
 *
 * เจ้าของงานเคาะ 2026-09-07 ให้นับจากวันจริง · เลือกสิ้นเดือนกันยายนให้ตรงกับชื่อบล็อก
 * ที่เขียนว่า "โปรโมชั่นเดือนนี้" · **แก้วันที่นี่ที่เดียว** ทั้งนาฬิกาและการตัดสินว่า
 * บล็อกยังขึ้นอยู่ไหมอ่านจากค่านี้ค่าเดียว
 *
 * **เลยวันนี้แล้วทั้งบล็อกไม่ขึ้น ไม่ใช่แค่นาฬิกาหาย** โปรโมชั่นที่หมดอายุแล้วแต่ยังโชว์
 * ราคาขีดฆ่าอยู่ คือการเสนอราคาที่ไม่มีอยู่จริง
 */
export const promoEndsAt = "2026-09-30T23:59:59+07:00";

/**
 * โปรโมชั่นยังไม่หมดอายุหรือยัง
 *
 * รับเวลาเข้ามาแทนที่จะอ่านนาฬิกาเอง ตามแบบแผนที่ `app-access.ts` ใช้อยู่ — ทำให้เขียน
 * เทสต์วันหมดอายุได้จริงโดยไม่ต้องรอถึงวันนั้น และทำให้คอมโพเนนต์ที่เรียกมันไม่มี
 * การอ่านนาฬิกาปนอยู่ในตัว ซึ่งเป็นสิ่งที่ตัวตรวจของ React ห้ามไว้ด้วยเหตุผลเดียวกัน
 */
export function isPromoLive(now: Date = new Date()): boolean {
  return now.getTime() < new Date(promoEndsAt).getTime();
}

export type PromoOffer = {
  /** slug ตามทะเบียนแอป — ป้ายสิทธิ์และถ้อยคำปุ่มให้ไปถามทะเบียนด้วย slug นี้ */
  slug: string;
  imageKey: VisualAssetKey;
  /** กลุ่มงานที่แสดงเหนือชื่อแอปบนการ์ด — ผืนวางไว้ 11px หนา 600 เหนือชื่อ */
  group: string;
  /** ราคาที่เสนอ หน่วยบาท */
  priceBaht: number;
  /** ราคาก่อนลด หน่วยบาท — null คือไม่มีราคาขีดฆ่า */
  wasBaht: number | null;
  /** หน่วยท้ายราคา เช่น ต่อเดือน — คำไทยตัวนี้นั่งอยู่ในช่องตัวเลข ต้องใช้ฟอนต์เดิมของเว็บ */
  unit: string;
};

/* **ถ้อยคำปุ่มไม่อยู่ในไฟล์นี้แล้ว** เคยมีฟิลด์ `ctaLabel` ที่เขียนว่า "เริ่มทดลองใช้"
   "สมัครสมาชิก" "เปิดใช้งาน" ซึ่งทั้งสามสัญญาว่ามีทางเข้า ทั้งที่ทะเบียนอาจยังไม่บอกว่าเปิด ·
   ถ้อยคำปุ่มเป็นคำแถลง (ADR 0015) จึงต้องมาจาก `describeCardClaims().cta` เหมือนที่
   แบนเนอร์สไลด์กับการ์ดแอปทั้งเว็บใช้ · ฟิลด์ที่นั่งรอในไฟล์ข้อมูลเป็นคำเชิญให้พิมพ์ตาย
   ลบทิ้ง 2026-09-07 ด้วยกฎเดียวกับที่ใช้กับ SearchIcon */

/**
 * **ราคาสมมติทั้งชุด รอเจ้าของงานยืนยัน**
 *
 * ราคาจริงที่ระบบมีวันนี้อยู่ที่ `@/lib/pricing` และเป็นราคาของแพลตฟอร์มทั้งใบ
 * ไม่ใช่ราคารายแอป — VIP รายเดือน รายปี และบัตรรายวัน เลขในตารางนี้ยังไม่มีอยู่จริง
 * ที่ไหนในระบบเลย
 *
 * เจ้าของงานเคาะ 2026-09-06 ให้คงราคาตัวอย่างไว้ก่อนและเก็บไว้ที่ไฟล์เดียวนี้ โดยเขาจะไป
 * แก้กติกาที่ ADR 0015 ทีหลัง จนกว่าจะถึงตอนนั้น **การ์ดโปรโมชั่นต้องมีหมายเหตุกำกับว่า
 * เป็นราคาตัวอย่าง** — หมายเหตุบรรทัดนั้นเป็นสิ่งเดียวที่กันไม่ให้ราคาสมมติถูกอ่าน
 * เป็นคำแถลงราคาจริง ห้ามเอาออกก่อนราคาจริงมาถึง
 */
export const promoOffers: readonly PromoOffer[] = [
  { slug: "estimeter", imageKey: "estimeter", group: "ประมาณราคางานอาคาร", priceBaht: 990, wasBaht: 1490, unit: "/ เดือน" },
  { slug: "rcopt", imageKey: "retaining_wall", group: "ออกแบบโครงสร้าง", priceBaht: 0, wasBaht: 590, unit: "/ เดือน" },
  { slug: "traffic-sign", imageKey: "traffic_sign", group: "งานอุปกรณ์ความปลอดภัย", priceBaht: 0, wasBaht: 390, unit: "/ เดือน" }
];

export type HeroSlide = {
  slug: string;
  imageKey: VisualAssetKey;
  /** บรรทัดบนของหัวเรื่อง ชื่อแอปตามทะเบียน ห้ามตัดบรรทัด */
  title: string;
  /** บรรทัดรอง บอกว่าแอปทำอะไร ห้ามตัดบรรทัด */
  subtitle: string;
  body: string;
  /** ถ้อยคำปุ่มรอง ใช้เมื่อทะเบียนบอกว่าเปิดจริงเท่านั้น ไม่งั้นปุ่มรองไม่ขึ้น */
  secondaryCtaLabel: string;
};

/**
 * สามสไลด์ของแบนเนอร์หลัก
 *
 * ป้ายมุมบนของแต่ละสไลด์เป็นคำแถลงเรื่องสิทธิ์ จึงไม่ได้เก็บไว้ที่นี่ — คอมโพเนนต์ต้องไป
 * เอาจากทะเบียนด้วย slug และถ้าทะเบียนยังไม่ประกาศแอปนั้น **สไลด์นั้นไม่มีป้าย** ไม่ใช่
 * หายไปทั้งสไลด์ (ADR 0015: เงียบ ไม่ใช่หาย)
 */
export const heroSlides: readonly HeroSlide[] = [
  {
    slug: "estimeter",
    imageKey: "estimeter",
    title: "ESTIMETR",
    subtitle: "ถอดแบบ · BOQ · ปร.4–6",
    body: "ประมาณราคาที่ย้อนดูที่มาได้ทุกบรรทัด ราคาอ้างอิงจาก สพฐ. 2569 และ ว 809",
    secondaryCtaLabel: "เริ่มทดลองใช้ฟรี"
  },
  {
    slug: "rcopt",
    imageKey: "retaining_wall",
    title: "RETAINING WALL",
    subtitle: "กำแพงกันดิน Cantilever",
    body: "หาหน้าตัดที่ผ่านเกณฑ์และประหยัดที่สุด พร้อมผลตรวจเสถียรภาพ",
    secondaryCtaLabel: "สมัครสมาชิกฟรี"
  },
  {
    slug: "traffic-sign",
    imageKey: "traffic_sign",
    title: "TRAFFIC SIGN",
    subtitle: "คำนวณวัสดุป้ายจราจร",
    body: "ระบุลักษณะป้าย ได้รายการวัสดุครบจน BOQ",
    secondaryCtaLabel: "เปิดใช้งาน"
  }
];

export type NewsDraft = {
  id: string;
  title: string;
  summary: string;
  /** ภาพปก — ยังไม่มีภาพจริงสักใบ ช่องที่ไม่มีภาพให้แสดงพื้นเทาเปล่า ห้ามหยิบภาพอื่นมาใส่แทน */
  cover: string | null;
};

/**
 * หัวข้อบทความสามเรื่อง เป็นร่างที่รอเจ้าของงานตรวจ
 *
 * **ยังไม่มีระบบบทความในเรพอ** จึงยังไม่มีปลายทางให้กด การ์ดชุดนี้ต้องเรนเดอร์เป็นการ์ด
 * ที่กดไม่ได้ ห้ามสร้าง route ปลอมขึ้นมารองรับ เพราะลิงก์ที่พาไปหน้าที่ไม่มีอยู่คือคำโกหก
 * แบบเดียวกับปุ่มที่สัญญาสิ่งที่ระบบทำไม่ได้
 */
export const newsDrafts: readonly NewsDraft[] = [
  {
    id: "obec-2569",
    title: "บัญชีราคาวัสดุและค่าแรง สพฐ. 2569 เปลี่ยนอะไรบ้าง",
    summary: "สรุปรายการที่ขยับ และวิธีดูว่าราคาที่ใช้อยู่มาจากฉบับไหน",
    cover: null
  },
  {
    id: "k-w109",
    title: "ค่า K ตาม ว 109 คิดยังไง และเมื่อไรที่ต้องคืนเงินหลวง",
    summary: "สัญญาแบบปรับราคาได้ไปได้สองทาง ไม่ได้มีแต่ทางที่ได้เงินเพิ่ม",
    cover: null
  },
  {
    id: "estimetr-trial",
    title: "ทดลองใช้ ESTIMETR ฟรี 7 วัน เริ่มจากตรงไหนดี",
    summary: "ตั้งโครงการ เปิดแบบ ยืนยันสเกล แล้วถอดปริมาณห้องแรกให้เสร็จในวันเดียว",
    cover: null
  }
];

/* เบอร์โทรกับ YouTube เคยพักไว้ที่นี่เป็นค่าตัวอย่างที่คัดมาจากเว็บอ้างอิง แล้วหลุดขึ้นแถบบน
   เป็นลิงก์ `tel:` ที่กดโทรออกได้จริง · เอาออกทั้งก้อน 2026-09-06 เพราะเบอร์ต่างจากราคาตรงที่
   ติดป้าย "ตัวอย่าง รอยืนยัน" ไม่ได้ คนเห็นเบอร์แล้วโทร ไม่ได้อ่านป้าย · ทางที่ถูกคือเพิ่มชนิด
   `phone` กับ `youtube` เข้าทะเบียนช่องทางติดต่อ แล้วให้ผู้ดูแลกรอกเอง ไม่ใช่กลับมาพักที่ไฟล์นี้ */
