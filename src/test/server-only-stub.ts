/**
 * ตัวแทนของแพ็กเกจ `server-only` ตอนรันเทสต์
 *
 * `import "server-only"` เป็นหมุดที่ทำให้ Next ล้มตอนสร้างบันเดิล ถ้ามีใครเผลอลากโมดูล
 * ฝั่งเซิร์ฟเวอร์ไปใช้ในไฟล์ที่วิ่งบนเบราว์เซอร์ ตัวแพ็กเกจจริงมีเฉพาะในโลกของ Next
 * ส่วน vitest วิ่งบน Node เปล่า ๆ จึงหาไม่เจอและล้มทั้งไฟล์เทสต์
 *
 * ทางแก้ที่ไม่เลือกคือถอดหมุดออกจากโมดูลเพื่อให้เทสต์รันได้ นั่นคือการรื้อรั้วเพราะเครื่องมือ
 * ตรวจไม่เข้าใจรั้ว ที่นี่จึงให้เทสต์รู้จักหมุดแทน ผูกไว้ที่ `vitest.config.ts`
 */
export {};
