# 2026-09-04 — ตัดรุ่น 0.99.0 · IP-235 คลิกเลือกพื้นที่ห้องเดินตามเส้นไกด์ของเจ้าของงานแล้ว

**สาขา** `initial-project/nextjs-foundation` · **รุ่น** 0.99.0 ตัดแล้ว **ยังไม่ปักธง ยังไม่ push**
**สถานะ** commit ล่าสุด `f7a2893` ต้นไม้สะอาด (เหลือ `next-env.d.ts` ที่ Next เขียนเอง ไม่ต้องสน)
**ทำไมปิดเซสชัน** context ใกล้เต็ม เจ้าของงานสั่งย้าย · ไม่มีอะไรพัง

---

## หนึ่ง — เซสชันใหม่ต้องทำอะไรต่อทันที (เรียงตามลำดับ)

1. `git status` ต้องสะอาด · `git log --oneline -5` ต้องเห็น `f7a2893` บนสุด
2. dev server: `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/` ถ้าไม่ตอบให้ `pnpm dev` เป็น background
3. **อ่าน skill `drawing-geometry` ก่อนแตะเรื่องไล่ขอบห้องทุกครั้ง** — มันเก็บกฎการแยกเส้น
   สามชนิด กับดักห้าข้อที่เคยพลาดจริงพร้อมวันที่ และกับดักของเครื่องมือทดสอบ
4. **ถามเจ้าของงานก่อนว่าผลคลิกเลือกห้องผ่านหรือยัง** เขายังไม่ได้ยืนยันรอบสุดท้าย
   (ดูหัวข้อสาม) · ถ้าผ่านแล้วค่อยไปงานถัดไปตามหัวข้อห้า
5. ถ้าเจ้าของงานสั่ง ให้ `pnpm release` ปักธงและ push รุ่น 0.99.0 (AGENTS.md ข้อ 9)

## สอง — สิ่งที่เสร็จวันนี้

### ตัดรุ่น 0.99.0 (`70e099a`)
IP-232 IP-233 IP-234 รวมเป็นหน่วย deploy ก้อนเดียว · roadmap 142 รายการ · CHANGELOG ·
ไม้ต่อ `2026-09-04-v0.99.0-marks-become-lines.md` · IP-234 ขั้น 8 จุดขายสองข้อ (`0dba7b4`)

### IP-235 คลิกเลือกพื้นที่ห้อง — งานหลักของวัน

| commit | เรื่อง |
|---|---|
| `7bd4cde` `62fa389` `0f9add9` | คำตัดสินของเจ้าของงานสามรอบ ลง `docs/plans/2026-09-03-room-area-explained-and-materials.md` |
| `29afabb` | คัดสัญลักษณ์ออกจากพื้นที่ (กลบรอยเว้า) + ถอด export ที่ทำให้ Fast Refresh พัง |
| `997ef7a` | `src/lib/grid-bay.ts` ตรรกะช่วงระหว่างแนวเสา **ยังไม่ต่อเข้าหน้าจอ** |
| `5746d02` | เชื่อมช่องประตูก่อนไล่สี + เปลี่ยนการขยาย-หดเป็นการแปลงระยะเชบีเชฟเพื่อความเร็ว |
| `6a64b9b` | **คัดเส้นก่อนไล่สี** ด่านสองชั้น ยาว ≥ 1.00 ม. และอยู่ในก้อนกว้าง ≥ 4.00 ม. + skill `drawing-geometry` |
| `e09df7a` | เสากั้นได้ ทั้งก้อนเล็กที่รูปร่างเป็นเสา และที่ว่างปิดสนิทข้างในเสาที่ชนผนัง |
| `263b614` | ที่ว่างต้องเต็มกรอบ ≥ 85% ไม่งั้นสามเหลี่ยมประตูกลายเป็นเสาปลอม |
| `3ebe78d` | แยกเกณฑ์ขนาดของเนื้อในเสาออกจากขนาดนอก เสาต้นเล็กจึงกั้นได้ |
| `79722a2` | **ไล่ขอบตามรอยต่อพิกเซล** ได้เหลี่ยมล้วนโดยโครงสร้าง เลิกใช้ Douglas-Peucker |
| `f7a2893` | **ดันขอบไปนั่งกึ่งกลางเส้นผนัง** ซึ่งคือผิวจริงของห้อง |

**เทสต์** 1,089 → **1,134** (`region-fill.test.ts` เพิ่มจาก 20 เป็น 48 ตัว)

### ค่าคงที่ที่ตั้งไว้ ทั้งหมดอยู่ใน `src/lib/region-fill.ts` พร้อมเหตุผล

| ค่า | ใช้ทำอะไร |
|---|---|
| `MIN_WALL_RUN_METRES = 1.0` | เส้นสั้นกว่านี้ไม่ใช่ผนัง คัดวงสวิง บานประตู ตัวอักษร เส้นประกริด |
| `MIN_STRUCTURE_METRES = 4.0` | ก้อนเล็กกว่านี้ไม่ใช่โครงสร้าง คัดกรอบป้ายกับสุขภัณฑ์ |
| `COLUMN_MIN/MAX_METRES = 0.15 / 0.80` | ช่วงขนาดของเสา · เนื้อในใช้เกณฑ์ที่ลดลง 4 พิกเซล |
| `COLUMN_TOUCH_METRES = 0.1` | เสาต้องชิดโครงผนัง กันสี่เหลี่ยมลอยกลางห้อง |
| `DOOR_BRIDGE_METRES = 1.0` | ช่องแคบกว่านี้ถือเป็นประตู เชื่อมให้ก่อนไล่สี |
| `OUTLINE_MIN_STEP_METRES = 0.05` | ขั้นสั้นกว่านี้เป็นความหยาบของภาพ ยุบทิ้ง |
| `WALL_SNAP_METRES = 0.07` | ระยะดันขอบไปหากึ่งกลางเส้นผนัง |

## สาม — สิ่งที่ยังไม่ได้ยืนยัน

- **เจ้าของงานยังไม่ได้ตอบว่าผลรอบสุดท้ายผ่านหรือยัง** ผมกดจริงเองแล้วได้
  ห้องพักพยาบาล **19.91 ตร.ม. เส้นรอบรูป 18.10 ม.** ซูม 385% ดูแล้วขอบนั่งบนเส้นผนัง
  ทั้งสี่ด้านและหักเป็นขั้นที่เสาทั้งสี่มุม · **ถามเขาก่อนทำอย่างอื่น**
- ทดสอบกับห้องเดียวเป็นหลัก (ห้องพักพยาบาล หน้า 7) ยังไม่ได้ไล่ทุกห้องในแบบ
- ยังไม่ได้ทดสอบกับแบบใบอื่นเลย ค่าคงที่ทั้งเจ็ดจูนจากแบบ 1:125 ใบเดียว

## สี่ — วิธีทดสอบ (ทำเองได้ ไม่ต้องรบกวนเจ้าของงาน)

```
http://localhost:3000/apps/estimeter/projects/24aaafab-51de-4a3a-915c-55db817a97c0/markup
```
ไฟล์แบบอยู่ที่ `C:\Users\moosu\Downloads\drawing.pdf` (32 หน้า หน้า 7 เป็นผังพื้น สเกล 1:125.29)
**ต้องคัดลอกเข้ามาในรีโปก่อนถึงจะ upload ผ่าน MCP ได้** เพราะ MCP เข้าถึงได้เฉพาะใน workspace
`cp /c/Users/moosu/Downloads/drawing.pdf .tmp-verify-drawing.pdf` แล้ว **ลบทิ้งทุกครั้งหลังเสร็จ**

ขั้นตอน: เปิดไฟล์ → กด `f` (พอดีจอ) → กด `k` (เครื่องมือห้อง) → คลิกกลางห้อง → กด `+` เพื่อซูมดูขอบ

**เครื่องมือส่องหน้ากากและขอบ** อยู่ในสคราตช์แพดของเซสชันนี้ (`mask-look.mjs` `outline-look.mjs`)
ถ้าหายให้เขียนใหม่ — มันเรนเดอร์หน้า 7 ด้วย pdfjs แล้ววาดหน้ากากที่คัดได้กับขอบที่ได้ทับลงบนแบบ
**แต่เชื่อไม่ได้เต็มร้อย** เพราะ pdfjs วาดตัวอักษรลง canvas ของ Node ไม่ได้แล้ว throw กลางคัน
ใช้ดูรายละเอียดหน้ากากได้ แต่การยืนยันผลต้องใช้เบราว์เซอร์

## ห้า — งานถัดไป (ให้เจ้าของงานเลือกลำดับ)

1. **ต่อ `grid-bay.ts` เข้าหน้าจอ** — ตรรกะเสร็จแล้วและเทสต์ผ่าน 12 ตัว แต่ยังไม่มีปุ่ม
   คำตัดสินครบแล้วในสเปกหัวข้อเจ็ด: ปุ่มใหม่ · คลิกจุดตัดสองจุดทแยงมุม · มุมที่ไม่ใช่จุดตัด
   ทำให้ป้ายลดชั้นเป็น "คนชี้จุดบนแบบ" · ป้ายไทย **"กึ่งกลางเสาถึงกึ่งกลางเสา"** รหัส `grid_bay`
   · การเพิ่มค่าใหม่ในทะเบียนที่มาของปริมาณ **ไม่ต้อง migration** แก้เจ็ดจุดในโค้ดกับเทสต์กับ ADR 0024
2. **ทดสอบไล่ทุกห้องในหน้า 7** แล้วดูว่าค่าคงที่ทั้งเจ็ดยังใช้ได้ทุกห้องไหม
3. งานไล่ขอบที่ยังค้างตามสเปกหัวข้อหก — ระบบเสนอเส้นปิดช่องประตูให้คนแก้ก่อนกดรับ
   (ตอนนี้เชื่อให้เลยโดยไม่ถาม) และการอ่านป้าย `F1` `C1` ด้วยผู้ช่วย AI
4. IP-187 แตกห้องเป็นวัสดุ — ติดที่เจ้าของงานต้องเปิดอ่านหลักเกณฑ์กรมบัญชีกลาง 2569 เอง
5. IP-230 มือถือ ยังเป็นแบบเท่านั้น ห้ามเริ่ม

## หก — วิธีที่ลองแล้วไม่ได้ผล (อย่าเดินซ้ำ)

- **แก้ปัญหาของเส้นด้วยการขยาย-หดก้อนพื้นที่** กลบสามเหลี่ยมเล็กได้ แต่เอากรอบป้ายกว้าง
  สองเมตรกับวงสวิงประตูไม่อยู่ และมนมุมจริงจนขอบลอยห่างผนัง · **การเพิ่มรัศมีไม่ใช่คำตอบ**
- **ไล่แก้ทีละจุดที่เจ้าของงานชี้** เสียเวลาแปดรอบ ทั้งที่เป็นความผิดพลาดสองอย่าง
  → อ่าน skill `drawing-geometry` หัวข้อ "เมื่อเจ้าของงานขีดเส้นไกด์ให้"
- **เชื่อการเรนเดอร์ตรวจใน Node เป็นหลักฐานสุดท้าย** pdfjs วาดตัวอักษรไม่ได้ ภาพขาดบางส่วน
- **MCP คลิกต่ำกว่าเป้า 46 พิกเซล** เคยทำให้สรุปผิดว่าปุ่มทั้งแถบตาย ทั้งที่คีย์ลัดใช้ได้
  → ดักจับ `pointerdown` เทียบพิกัดจริงก่อนสรุปเสมอ · ชดเชยด้วยการวางกล่องเล็งสูงกว่าเป้า 46
- **`pnpm release`** ยังห้ามรันจนกว่าเจ้าของงานสั่ง

## เจ็ด — ของนอกรีโปที่เปลี่ยนวันนี้

- `~/.claude/projects/.../memory/ask-with-options-not-prose.md` เพิ่มว่ากฎนี้ชนะฟอร์แมตของ skill
  เจ้าของงานทักซ้ำเป็นครั้งที่สอง — **ทุกคำถามต้องเด้งเป็นตัวเลือกให้กด ห้ามให้พิมพ์ตอบ**
- ล้างข้อมูลหน้า 7 ของโครงการทดสอบตามคำสั่งเจ้าของงาน (รอยวัด สเกล แนวเสา ตำแหน่งซูม
  และรายการถอดปริมาณสามตัว) · คอนกรีตฐานรากของหน้า 17 ยังอยู่
- เจ้าของงานตั้งสเกลหน้า 7 ใหม่เองเป็น **1:125.29**
