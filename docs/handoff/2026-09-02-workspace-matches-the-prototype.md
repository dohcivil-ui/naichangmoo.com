# 2026-09-02 — หน้าทำงานวัดปริมาณตรงกับต้นแบบ และแก้บั๊กเครื่องมือวัดสิบข้อ

**วันที่** 2026-09-02 · **สาขา** `initial-project/nextjs-foundation` · **รายการ** IP-227 IP-228
**รุ่น** ยังไม่ตัดรุ่น อยู่ที่ 0.97.0 · **สถานะ** ยังไม่ commit งานอยู่ในไฟล์ 13 ไฟล์

รอบนี้เริ่มจากพิสูจน์ว่าเครื่องมือวัดบนแบบทำงานถูกจริงด้วยการกดมือ แล้วต่อด้วยการยกหน้าจอ
ทั้งหน้าให้ตรงกับต้นแบบที่เจ้าของงานวางไว้ ปิดท้ายด้วยการวางแผนท่ออ่านแบบสามชั้น

---

## หนึ่ง — สิ่งที่พิสูจน์ด้วยการกดมือบนแบบจริง

เปิด `km/kusumal_hospital/แบบก่อสร้างอาคารฟอกไต ปุญโญภาส (ฉบับสมบรู.pdf` หน้า 7 ด้วย Edge
ผ่าน chrome-devtools แล้วกดใช้ทุกเครื่องมือ ไม่ได้ดูแต่ผลเทสต์

| สิ่งที่วัด | ผล |
|---|---|
| ตั้งสเกลจากเส้นกริดที่แบบเขียน 5.00 ม. | **1:125 พอดี** ตรงกับที่หัวแบบระบุ |
| วัดช่วงกริดถัดไป | **5.00 ม. พอดี** |
| พื้นที่สี่เหลี่ยมกับพื้นที่หลายเหลี่ยมบนห้องเดียวกัน | 20.10 กับ 19.91 ตร.ม. ต่างกัน 0.9% |
| ซูมจาก 200% เหลือ 100% | ตัวเลขไม่ขยับสักตัว รูปยังทับตรงห้องเดิม |
| เปลี่ยนไปหน้า 8 | รอยวัดหายหมด สเกลกลับเป็นยังไม่ตั้ง เครื่องมือล็อกใหม่ห้าตัว |

**พิกัดที่ใช้ทดสอบซ้ำได้** เส้นกริดแนวตั้งบนหน้า 7 อยู่ที่พิกัดหน้ากระดาษ
**177.84 · 291.15 · 404.47** ระยะระหว่างเส้น 113.3 หน่วยกระดาษ ต่อช่วง 5.00 เมตร

---

## สอง — บั๊กที่เจอจากการกดมือ สิบข้อ แก้ครบ

| # | อาการ | ผลกับตัวเลข |
|---|---|---|
| 1 | คลิกขวาเพื่อจบการวัดใส่จุดผีก่อนจบ | เส้น 5.02 ม. กลายเป็น **7.64 ม.** |
| 2 | กด Enter ตอนถือเครื่องมือเลือก | ได้แถวขยะไม่มีชนิดไม่มีค่า |
| 3 | ดูดจุดถูกใช้กับจุดตั้งต้นของเลือกพื้นที่ห้อง | ดูดไปติดผนังแล้วขึ้นว่าคลิกโดนเส้นทุกครั้ง ใช้ไม่ได้เลย |
| 4 | สีทะลุออกนอกห้องแล้วเงียบ | ได้ 266 ตร.ม. จากห้อง 20 ตร.ม. โดยไม่เตือน |
| 5 | เครื่องมือเลื่อน ลากแล้วหน้าไม่ขยับ | ปุ่มตาย |
| 6 | เครื่องมือเลือก คลิกรูปแล้วไม่เลือก | เลือกได้จากตารางทางเดียว |
| 7 | กด Enter จบการวัดแล้วแบบซูมออกไปด้วย | โฟกัสค้างที่ปุ่มซูม คีย์ลัดไปกดปุ่มนั้นซ้ำ |
| 8 | pdf.js ทิ้ง promise ที่ถูกยกเลิกไว้ | คอนโซลขึ้น error ทุกครั้งที่ซูม |
| 9 | แถบยืนยันพื้นที่ห้องทับพื้นที่ทำงาน | กดปุ่มยืนยันไม่ได้เลย |
| 10 | ซ่อนรางหน้าแบบแล้วพื้นที่วาดกว้างศูนย์ | ชิ้นที่ `display:none` หลุดจากการวางกริด ลูกที่เหลือเลื่อนคอลัมน์ |

ข้อ 10 เจอเพราะตรวจ responsive ที่ 761px ถ้าไม่ตรวจจะโผล่ตอนผู้ใช้กดปุ่มซ่อนราง

**คำตัดสินของเจ้าของงานที่ทำให้เกิดการแก้ข้อ 4 กับการนับจำนวน**
เพดานสีทะลุลดจาก 60% เหลือ **25% ของหน้า** และ**ต้องกดยืนยันรูปทุกครั้ง**ก่อนเข้ารายการวัด ·
เครื่องมือนับจำนวนสะสมหลายจุดในแถวเดียว จบด้วยคลิกขวา ไม่ใช่คลิกละแถว

---

## สาม — หน้าจอถูกยกใหม่ทั้งหน้าให้ตรงต้นแบบ

ต้นแบบคือ `.design/estimeter-viewer/viewer-controls-prototype.html` ซึ่งเจ้าของงานยืนยันเองว่า
คือภาพที่เขาต้องการ ของเดิมไปนั่งอยู่ในเปลือกของหน้าแรกที่มีหัวเว็บกับท้ายเว็บ เหลือพื้นที่วาด 70vh
ที่ต้องเลื่อนหน้าเว็บลงไปหา

**เปลือกใหม่** `AppShell` ได้โหมดที่สาม `mode="workspace"` — เหลือแถบเดียว (ทางกลับบ้าน ·
แอปทั้งหมด · ชื่อแอป · ป้ายสิทธิ์ · เมนูบัญชี) แล้วยกที่เหลือทั้งจอให้เนื้องาน ไม่มีหัวแอป ไม่มีท้ายเว็บ

**แยก route group** `src/app/apps/estimeter/(standard)/` กับ `(workspace)/` เพราะ layout ของ
Next.js ซ้อนกันเสมอ ลูกถอด layout ของแม่ไม่ได้ · **URL ไม่เปลี่ยนสักอัน** route group ไม่โผล่ใน URL
· **กับดัก** `git mv` ทั้งโฟลเดอร์ล้มด้วย Permission denied ขณะ dev server เปิดอยู่ ต้องย้ายทีละไฟล์

**ผังสี่แถวของหน้าทำงาน** แถบแอป (ทางกลับโครงการ ชื่อโครงการ เปิดไฟล์ ย้อนกลับ ทำซ้ำ) ·
แถบเครื่องมือไอคอนเก้าตัวพร้อมป้ายลอยบอกชื่อ คำอธิบาย และคีย์ลัด · ราง-ที่จับ-แบบ-ที่จับ-แผงขวา
ลากปรับความกว้างได้ · แถบสถานะล่าง

**กล้องเปลี่ยนวิธีทำงาน** จากเดิมที่เลื่อนด้วย scrollbar เป็น `translate + scale` แบบต้นแบบ —
ซูมที่ตำแหน่งเมาส์ ลากเลื่อน ดับเบิลคลิกซูม พอดีกรอบอัตโนมัติเมื่อเปิดไฟล์และเปลี่ยนหน้า

**คีย์ลัดชุดใหม่ตามต้นแบบ** `V H S L P R G K C` · `N` ดูดจุด · `1` ราง · `2` แผงขวา ·
`+ - 0` ซูม · `F` พอดีกรอบ · `Esc` ยกเลิก · `Enter` จบการวัด · `Ctrl+Z` `Ctrl+Shift+Z`

**สองอย่างที่จงใจไม่ทำตามต้นแบบ** แถบบนสุดยังมีทางกลับหน้าแรกกับแอปทั้งหมด เพราะ skill
`app-shell` บังคับว่าทางออกต้องเห็นตลอดเวลา · แผงผู้ช่วย AI ในต้นแบบยังไม่ทำ เพราะเนื้อหาในนั้น
เป็นตัวอย่างสมมติ ของจริงต้องมาจากทักษะ extract และต้องเสียบผ่านแผงผู้ช่วยกลาง

---

## สี่ — ตรวจ responsive ครบหกความกว้าง

360 · 640 · 721 · 761 · 1024 · 1440 — **ไม่มีการเลื่อนแนวนอนสักความกว้าง**
≤1024 ซ่อนรางอัตโนมัติ · ≤760 แผงรายการย้ายลงล่าง · เดสก์ท็อปปุ่มซ่อนราง/ซ่อนแผงคืนพื้นที่ให้แบบ
ได้เต็ม 1440 · หน้าต่างเบราว์เซอร์จริงย่อได้ต่ำสุดราว 493px ต้องใช้การจำลองหน้าจอเพื่อตรวจที่ 360

---

## ห้า — ข้อเท็จจริงเรื่องความคมชัดที่วัดแล้ววันนี้ และยังไม่ได้แก้

เจ้าของงานทักว่าแบบที่แสดงไม่คมพอ วัดของจริงแล้วได้ตัวเลขนี้

**เราวาดภาพละเอียดเพียง 0.53 พิกเซลต่อหนึ่งพิกเซลจริงของจอ** — จอของเครื่องนี้มีความหนาแน่น
1.89 เท่า แต่โค้ดตั้งขนาดผืนวาดเท่ากับพิกเซล CSS ตรง ๆ ไม่เคยคูณด้วยความหนาแน่นจอ
เบราว์เซอร์จึงยืดภาพขึ้นเกือบสองเท่า `devicePixelRatio` ไม่ปรากฏใน `src/` เลยแม้แต่ครั้งเดียว

**แสดงแบบเวกเตอร์ทำไม่ได้** `pdfjs-dist` ที่ติดตั้งคือ **4.10.38** และรุ่น 4 ถอด SVG backend
ออกไปแล้ว ค้นทั้งแพ็กเกจไม่พบ `SVGGraphics` เลย เหลือแต่ `DOMSVGFactory` ซึ่งเป็นตัวสร้าง
element เปล่าให้ annotation layer ไม่ใช่ตัววาดเนื้อหา **ทุกหน้าต้องแปลงเป็นภาพเสมอ**

**ความละเอียดที่ต้องการต่างกันตามคนใช้ สามทาง**

| ใครใช้ | ต้องการ | ตอนนี้ได้ |
|---|---|---|
| ตาคน | เท่าความหนาแน่นจอ | 0.53 เท่า เบลอ |
| ดูดจุดและไล่พื้นที่ห้อง | ทั้งหน้า ความละเอียดคงที่พอสมควร | ผูกกับระดับซูม เปลี่ยนไปมา |
| OCR | **300 dpi** (เจ้าของงานเคาะ 2026-09-02) | เพดาน 2.5 = **180 dpi ต่ำกว่าที่เครื่องอ่านออก** |

**ตัวเลขหน่วยความจำที่คำนวณไว้แล้ว** A3 ทั้งหน้า — 180 dpi 25 MB · 300 dpi 70 MB ·
340 dpi (เพดานเดิมคูณความหนาแน่นจอ) 90 MB · เฉพาะส่วนที่มองเห็นบนจอ **8 MB คงที่ทุกระดับซูม**

**เพดานที่ไลบรารีและเบราว์เซอร์บังคับ** 16,384 พิกเซลต่อด้าน · ~32 ล้านพิกเซลต่อผืนวาดบนเดสก์ท็อป
และ 5 ล้านพิกเซลบนมือถือ · pdfjs มีคลาส `OutputScale` ที่อ่าน `devicePixelRatio` ให้แล้ว ใช้แทน
การเขียนเองได้ · เรนเดอร์เฉพาะบางส่วนของหน้าทำได้ด้วย `getViewport({ offsetX, offsetY })`
หรือ `transform` ของ `render()`

**ของที่ต้องแก้ที่เจอระหว่างทาง** `page.render()` ในโค้ดส่งฟิลด์ `canvas` เข้าไปด้วย
(`drawing-markup.tsx:324` และ `:373`) ซึ่ง **ไม่มีในรุ่น 4.10.38** ถูกละทิ้งเงียบ ๆ
เป็นฟิลด์ของ pdf.js รุ่น 5 · และผืนวาดที่แสดงผลใช้ `willReadFrequently: true` ซึ่งบังคับให้
วาดด้วย CPU ไม่ใช่ GPU ควรใช้เฉพาะผืนวาดที่ต้องอ่านพิกเซลกลับเท่านั้น

---

## หก — คำตัดสินของเจ้าของงานในรอบนี้

| เรื่อง | คำตัดสิน |
|---|---|
| เพดานสีทะลุออกนอกห้อง | ลดเหลือ 25% ของหน้า **และ**ต้องกดยืนยันรูปทุกครั้ง |
| เครื่องมือนับจำนวน | สะสมหลายจุดในแถวเดียว จบด้วยคลิกขวา |
| เครื่องมือเลือกกับเลื่อน | ต้องใช้ได้จริงทั้งคู่ |
| หน้าตาหน้าทำงาน | ยกทั้งหน้าให้เหมือนต้นแบบรอบเดียว ไม่ทำครึ่ง ๆ |
| ความคมชัด | **ให้เป็นตัวเลือกที่ผู้ใช้เปิดปิดเอง** คงโหมดเบาไว้ และเพิ่มโหมดคมมาก |
| ความละเอียดภาพสำหรับ OCR | **300 dpi** |
| ภาพที่ส่งออกไป OCR | เฉพาะกรอบที่ครอบ และต้องกดยินยอมรายโครงการ |
| ปุ่มเพิ่ม OCR API | อยู่ในหน้าผู้ดูแล วางกุญแจได้จริงโดยไม่ต้องแก้ไฟล์ |
| ที่มาของภาพ OCR | เบราว์เซอร์เรนเดอร์แล้วส่งขึ้นเฉพาะภาพ ไม่ต้องรอ R2 |

---

## เจ็ด — สิ่งที่ยังไม่ได้ทำ เรียงตามที่ควรทำก่อน

1. **ความคมชัดเป็นตัวเลือกของผู้ใช้** — คูณ `devicePixelRatio` เข้ากับขนาดผืนวาด และแยกผืนวาด
   สำหรับแสดงผลออกจากผืนวาดสำหรับวิเคราะห์ · ต้องมีปุ่มเปิดปิดในแถบเครื่องมือ เพราะเจ้าของงาน
   บอกว่าความคมสำคัญตอนตั้งสเกลและลากเส้น แต่ไม่ต้องเปิดตลอดเวลา
2. **การดูดจุดที่ยังขาดไป** — ตอนนี้ดูดเข้าหา "พิกเซลที่เข้มที่สุด" เท่านั้น ของที่เจ้าของงานมีอยู่แล้ว
   ในแอปเดิมที่ `doh-thai.com/estimate/` และยังไม่ได้ยกมาคือ **ดูดเข้าจุดตัดของกริด · ดูดเข้าปลายเส้น ·
   บังคับตั้งฉาก** — สามอย่างนี้คือสิ่งที่ทำให้สอบเทียบสเกลและลากเส้นแม่นจริง
3. **แผนท่ออ่านแบบสามชั้นสิบเอ็ดก้อน** เขียนไว้แล้วและเจ้าของงานอนุมัติแล้ว อยู่ที่
   `C:\Users\moosu\.claude\plans\ocr-api-nested-dusk.md` — ก้อน 1 คือตาราง `drawing_calibrations`
   ก้อน 3 คือบันทึกรายการวัดลงฐานข้อมูล (ตอนนี้ปิดหน้าเว็บแล้วหาย) ก้อน 6 คือจุดวัดผลก่อนจ่ายเงินให้ OCR
4. **ADR 0024** เรื่องภาพแบบของลูกค้าออกนอกระบบได้เมื่อใด — `AGENTS.md` ข้อ 2 บอกว่า ADR เป็นงาน
   ของโมเดลวางแผน ไม่ใช่ของผู้ลงมือ และต้องเกิดก่อนโค้ด · ต้องแก้ `docs/security/source-and-ai-boundary-review-2026-08-22.md:25`
   ในรุ่นเดียวกัน เพราะบรรทัดนั้นห้ามเด็ดขาด ขัดกับบรรทัด `:27` ที่บอกว่าทำได้ถ้าลดข้อมูลก่อน
5. **แก้เอกสารที่บันทึกผิด** `docs/requirements/estimeter-drawing-markup.md:179` และ
   `docs/handoff/2026-09-01-markup-on-drawings.md:37` เขียนว่าตัวเลขที่ดึงจากชั้นข้อความเพี้ยน
   **ไม่จริง** วัดใหม่ทั้งไฟล์แล้วไม่พบตัวเลขเสียหายสักตัว ที่เพี้ยนคือวรรณยุกต์กับการันต์ไทยเจ็ดรหัส 68 ครั้ง
6. **สองอย่างที่รอเจ้าของงานทำเอง** รัน `bash scripts/setup-r2.sh` · ตัดสิน IP-109 ว่าสมาชิกที่หมดอายุ
   พิมพ์เอกสารแบบอ่านอย่างเดียวได้หรือไม่ ซึ่งกั้นงานส่งออก ปร.4 ปร.5 ปร.6 ทั้งหมด

---

## แปด — ไฟล์ที่เปลี่ยนและการตรวจ

**ย้ายที่ 6 ไฟล์** เส้นทางของ ESTIMETR เข้าสองกลุ่ม `(standard)` กับ `(workspace)`
**ไฟล์ใหม่ 1 ไฟล์** `src/app/apps/estimeter/(workspace)/layout.tsx`
**แก้ 8 ไฟล์** `drawing-markup.tsx` (เขียนใหม่ทั้งไฟล์) · `globals.css` · `app-shell.tsx` ·
`site-header.tsx` · `drawing-measurement.ts` และเทสต์ · `region-fill.ts` และเทสต์

**การตรวจ** `pnpm lint` `pnpm typecheck` เขียว · `pnpm test` **920 ตัวผ่าน** เพิ่มใหม่ 12 ตัว ·
`pnpm build` ผ่าน · เส้นทางทั้งห้าของ ESTIMETR ตอบ 200 เหมือนเดิม · กดจริงในเบราว์เซอร์ Edge
พร้อมภาพหน้าจอทุกขั้น

**ความเสี่ยง** การย้าย route group ทำให้ `.next/types` ค้างของเก่า ถ้า typecheck ฟ้องว่าหาโมดูล
`src/app/apps/estimeter/page.js` ไม่เจอ ให้ลบ `.next/types` แล้วรันใหม่ ไม่ใช่ความผิดของโค้ด

**การย้อนกลับ** ยังไม่ commit ย้อนได้ด้วย `git checkout -- .` และลบไฟล์ใหม่หนึ่งไฟล์
ไม่มี migration ไม่มีการเปลี่ยนโครงสร้างข้อมูล · มีการแก้ฐานข้อมูลบนเครื่องพัฒนาหนึ่งแถว คือ
ต่ออายุสิทธิ์ทดลอง ESTIMETR ของ `org_personal_7ewfEmMaFOOqqSsrMLwIGBYvKr4UxS3M`
ถึง 2026-10-01 ตามที่เจ้าของงานอนุญาต

---

## เก้า — คำสั่งเปิดเซสชันถัดไป

อยู่ที่ท้ายบทสนทนาของรอบนี้ และสาระสำคัญคือ อ่านเอกสารฉบับนี้ก่อน แล้วอ่านแผนที่
`C:\Users\moosu\.claude\plans\ocr-api-nested-dusk.md` · เรียก `/grill-with-docs` ก่อนแตะ source
ตาม `AGENTS.md` ข้อ 1 · งานแรกคือความคมชัดที่ผู้ใช้เปิดปิดเองได้ พร้อมการดูดจุดสามแบบที่ยังขาด
