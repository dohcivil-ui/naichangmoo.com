# 2026-09-04 (ดึก) — IP-235 พื้นที่ห้องมาจากไหน และหน้าแรกของแอปที่ต้องออกแบบใหม่

**สาขา** `initial-project/nextjs-foundation` · **รุ่น** 0.99.0 ยังไม่ปักธง **ยังไม่ push**
**ทำไมปิดเซสชัน** บทสนทนายาว เจ้าของงานสั่งไว้ว่าให้ทำไม้ต่อแล้วย้ายเซสชัน

---

## หนึ่ง — เซสชันใหม่ต้องทำอะไรต่อทันที

1. `git log --oneline -6` ต้องเห็นหกคอมมิตนี้บนสุด `05d240f` `a0de550` `2d01b69` `3eeb29e`
   `c832f5f` `28d0baa` · `git status` เหลือแค่ `next-env.d.ts` กับ `.design/` `image/` ที่ค้างมานาน
2. **ลบ `.tmp-verify-drawing.pdf` ถ้ายังอยู่** มันคือสำเนาของ `C:\Users\moosu\Downloads\drawing.pdf`
   ที่ต้องวางในรีโปเพราะ MCP อัปโหลดไฟล์นอก workspace root ไม่ได้ · **ไม่อยู่ใน `.gitignore`**
3. dev server: `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/`
   · หน้า markup ตอบ 500 "Jest worker" คือ dev server ค้างข้ามคืน ปิดโปรเซส node แล้วเปิดใหม่
4. **งานถัดไปคือหน้าแรกของแอป** ดูหัวข้อห้า เจ้าของงานเคาะแนวไว้แล้ว ต้นแบบทำแล้ว รอเขาดู

## สอง — คำตอบเรื่องพื้นที่ห้อง วัดมาทั้งหมด ไม่มีตัวไหนเดา

เจ้าของงานถามว่าห้องน้ำผู้ป่วยชายที่จอโชว์ 4.00 ตร.ม. มาจากไหน ในเมื่อเส้นบอกระยะเขียน
2.50 × 2.00 · **รอบแรกผมตอบผิด** อ้างว่า "ผนัง 0.25 หักด้านละ 0.125" ซึ่งถ้าคิดตามนั้นได้ 3.94
ไม่ใช่ 4.00 · ที่ผิดคือหยิบ `WALL_THICKNESS_METRES = 0.25` มาใช้ผิดความหมาย มันคือ**ระยะค้นหา
คู่เส้นผนัง** ไม่ใช่ความหนาผนังของแบบนี้ · เจ้าของงานทักว่า "คิดตกหล่นไปเยอะ" และถูก

**วิธีวัด** ภาพชั้นวิเคราะห์หน้า 7 ขนาด 2382 × 1684 = A3 (1190.52 × 841.92 pt) ที่ 2 เท่า
สเกล 1:125 → **45.354 พิกเซลต่อเมตร** (`2 ÷ (125 ÷ 2834.6457)`)

### ห้องน้ำผู้ป่วยชาย — สามเลข วัดจากเส้นจริงในภาพ

| วัดจากอะไร | แนวนอน | แนวตั้ง | เมตร | พื้นที่ |
|---|---|---|---|---|
| แนวเสา (เส้นประจุด) | 1172.5 → 1285.5 = 113 px | 954.5 → 1045.5 = 91 px | 2.491 × 2.006 | **5.00** |
| ผิวนอกผนัง | 1174 → 1288 = 114 px | 957 → 1048 = 91 px | 2.514 × 2.006 | 5.04 |
| ผิวในผนัง = ขอบที่ไล่ได้ | 1180 → 1283 = 103 px | 963 → 1043 = 80 px | 2.271 × 1.764 | **4.01** |

แนวเสาที่วัดได้ตรงกับ 2.50 × 2.00 ที่แบบเขียน คลาด 0.4% ซึ่งคือความหยาบของภาพ (1 px = 0.022 ม.)
**แนวเสาวัดจากเส้นประจุดจริง ในช่วงนอกตัวอาคาร** จึงไม่ปนกับผนังหรือกระเบื้อง

### ของที่ตกหล่นและสำคัญ — ผนังวางบนแนวเสาไม่เหมือนกันแม้ในห้องเดียว

| ผนัง | หมึกอยู่ที่ | แนวเสาอยู่ที่ | ความสัมพันธ์ |
|---|---|---|---|
| ขวา | 1283–1288 | 1285.5 | คร่อมกึ่งกลางพอดี |
| ล่าง | 1043–1048 | 1045.5 | คร่อมกึ่งกลางพอดี |
| ซ้าย | 1174–1179 | 1172.5 | แนวเสาอยู่นอกผนังทั้งก้อน |
| บน | 957–962 | 954.5 | แนวเสาอยู่นอกผนังทั้งก้อน |

**ผลคือไม่มีสูตรหักผนังคงที่** ที่แปลง 5.00 เป็น 4.01 ได้ · ถ้าหักครึ่งผนังสี่ด้านตามทฤษฎีได้
2.403 × 1.896 = 4.56 ซึ่งไม่ตรงกับของจริงเลย

### และส่วนต่างไม่คงที่ระหว่างห้อง

| ห้อง | แนวเสา | ผิวใน | ต่าง |
|---|---|---|---|
| ห้องน้ำผู้ป่วยชาย | 5.00 (2.491 × 2.006) | 4.01 (2.271 × 1.764) | 0.99 = **20%** |
| ห้องแยก | 7.50 (2.999 × 2.491) | 7.35 (2.977 × 2.469) | 0.15 = **2%** |

**นี่คือเรื่องที่ต้องบอกเจ้าของงาน** คำตัดสิน 2026-09-04 ของเขาที่ว่า "ส่วนต่างทำหน้าที่เป็น
ค่าเผื่อในตัว ไม่ต้องบวกเผื่ออีก" มาจากห้องแยกซึ่งต่าง 2–6% · แต่กับห้องน้ำเล็ก ๆ การใช้เลข
แนวเสาคือเผื่อไป 25% เพราะเส้นรอบผนังใหญ่เมื่อเทียบกับพื้นที่ · **ยังไม่ได้ถามเขา ต้องถาม**

### เส้นบอกระยะอ่านอัตโนมัติไม่ได้

พิสูจน์แล้วด้วยการดึง text layer ของหน้า 7 — มี text item แค่ **12 ตัว** และ **ไม่มีตัวเลข
รูปแบบ `d.dd` เลยสักตัว** เลขบอกระยะทั้งหมดถูกวาดเป็นเส้น ไม่ใช่ตัวอักษร · จะอ่านอัตโนมัติ
ต้อง OCR ซึ่งต้อง 300 dpi และต้องมีคนกดรับ ไม่ใช่เลขสุดท้ายที่ไหลเข้า BOQ เอง

**ทางที่ไม่มั่วซึ่งมีอยู่แล้วในโค้ด** `src/lib/grid-bay.ts` เขียนเสร็จพร้อมเทสต์ รับสองมุมตรงข้าม
หา bay ที่ล้อมอยู่ และ **เอาเลขที่คนพิมพ์จาก `StatedDimension` มาก่อนเลขที่คำนวณได้**
มี `bayExplanation()` สำหรับ backup sheet ด้วย · **ยังไม่ได้ต่อเข้าหน้าจอ** นี่คืองานที่ค้าง

## สาม — สิ่งที่เสร็จวันนี้ หกคอมมิต

| คอมมิต | เรื่อง |
|---|---|
| `28d0baa` | ตัดเส้นรอบรูปออกจากทุกที่ที่คนอ่าน (แถบยืนยัน · บรรทัดวิธีคิด · แผงรายการวัด) เจ้าของงานสั่ง · ค่ายังคำนวณอยู่ใน `drawing-measurement.ts` ไม่แตะ schema |
| `c832f5f` | `structuralBarrier` จำหน้ากากผนังไว้กับ `GreyImage` ด้วย WeakMap · วัดจริงบนหน้า 7 คลิกแรก 327 ms คลิกถัดไป 38 ms และ 12 ms · เทสต์ใหม่สี่ข้อ |
| `3eeb29e` | ชื่อแอปเป็น "แอปผู้ช่วยประมาณราคาก่อสร้างงานอาคาร" |
| `2d01b69` | การ์ดลำดับงานบอกสถานะขั้นเปิดแบบตามจริง มีปุ่มเข้าหน้าแบบ และถอดผู้ช่วยจับคู่ BOQ ออก |
| `a0de550` | ถอดพื้นที่สาธิต `EstimationWorkspace` ออกจากหน้าแรก |
| `05d240f` | ชื่อสี่ขั้นเป็นประโยค ใช้ชุดเดียวกันทั้งแอป · "ตั้งค่าโครงการ" แทน "ตั้งโครงการ" |

## สี่ — ข้อ 2 ของไม้ต่อก่อน: ห้อง RO ผนังไม่ได้หาย แบบไม่ได้เขียนผนัง

ไล่ด้วยวิธีเดิม bundle `region-fill.ts` เข้าแท็บทดสอบแล้วดูหน้ากากเป็นภาพ

**สแกนทั้งก้อนที่รวมกัน** (x 1266–1628, y 500–816) หาเส้นตรงยาว ≥ 1 ม. ที่หน้ากากไม่รับ
เจอสี่คู่ ทุกคู่ห่างกัน 22–23 px พอดี ซึ่งคือ**กรอบป้ายชนิดพื้นและฝ้า** (`F1+0.60 C1`)
กับเศษของ**เส้นประจุด** ที่ y=616 · **ไม่มีเส้นไหนเป็นผนังที่กฎมองข้าม**

ซูม 5 เท่าตรงแนวที่ควรเป็นผนังคั่น เห็นชัดว่าเป็น**เส้นประจุด (เส้นแกน)** ไม่ใช่ผนัง
ห้อง RO กับห้องเตรียมตัวกรองเป็นที่โล่งเดียวกันในแบบ แยกด้วยเส้นแกนกับป้ายชนิดฝ้าคนละตัว
(C2 กับ C1) เท่านั้น · **ห้องเก็บน้ำยากับโถงเหมือนกัน** สแกนแล้วเจอแต่กรอบป้ายกับเส้นแกน

**สรุป หน้ากากทำถูก** ไม่ต้องเพิ่มกฎผนังชนิดใหม่ · ถ้าเจ้าของงานยืนยันว่าตรงนั้นมีผนังจริง
แปลว่าต้องดูแบบแผ่นอื่น ไม่ใช่แก้ตัวไล่ขอบ

## ห้า — งานถัดไป เรียงตามที่เจ้าของงานสั่ง

1. **หน้าแรกของแอป ออกแบบใหม่** เขาบอกว่า "ห่วยมาก คนเข้ามาใหม่ทำงานไม่ง่าย"
   · เคาะแล้วสามข้อ **สองคอลัมน์ งานซ้าย ผู้ช่วยขวา** · **ผู้ช่วยเริ่มด้วยปุ่ม พิมพ์เองก็ได้**
   · **ถอดพื้นที่สาธิตออก** (ทำแล้ว `a0de550`)
   · ต้นแบบอยู่ที่ `.design/estimeter-home/home-with-guide-bot.html` **ส่งให้เขาดูแล้ว รอความเห็น**
   · ยังไม่ได้ต่อเข้าแอปจริง ห้ามต่อจนกว่าเขาจะบอกว่าต้นแบบผ่าน
2. **ผู้ช่วยในหน้าแบบ** เขาสั่งว่า "มันต้องมีผู้ช่วยตอนเข้าไปหน้าแบบนู่น อาจจะให้ตรวจสอบแบบ
   แล้วช่วยถอดปริมาณวัสดุ หรือช่วยคิดพื้นที่ หรืออะไรก็ตามที่คนคิดให้น้อยที่สุด"
   · **ยังไม่ได้ออกแบบ ต้อง grill ก่อน** · ข้อจำกัดที่ต้องเคารพ ตัวเลขมาจากสูตรที่ตรวจซ้ำได้
   ไม่ใช่จากแบบจำลอง (`method-claims.ts` ข้อแรก) แบบจำลองอยู่ได้เฉพาะขั้นที่คนกดรับทีละรายการ
3. **ต่อ `grid-bay.ts` เข้าหน้าจอ** เพื่อให้เลขแนวเสาขึ้นคู่กับเลขผิวใน และบรรทัดวิธีคิด
   ลง backup sheet ได้จริง · **นี่คือคำตอบของเรื่องพื้นที่ห้องที่เขาคาใจ** ไม่ใช่การแก้ตัวไล่ขอบ
4. **ถามเรื่องค่าเผื่อ** ส่วนต่างแนวเสากับผิวในเป็น 20% ในห้องเล็กและ 2% ในห้องใหญ่
   คำตัดสินเดิมที่ว่าไม่ต้องบวกเผื่ออีกมาจากห้องใหญ่ · ต้องให้เขาเคาะว่าห้องเล็กเอาอย่างไร
5. **เก็บไฟล์แบบไว้ในระบบ** ตอนนี้ PDF ไม่ได้เก็บ ผู้ใช้ต้องเปิดจากเครื่องทุกครั้ง
   เป็นสาเหตุหลักที่เขารู้สึกว่างานกระโดดไปกระโดดมา · แตะ R2 กับ security รอเขาตัดสิน
6. งานเดิมที่ยังไม่แตะ IP-187 · IP-230 ห้ามเริ่ม

## หก — กติกาเรื่องคำที่เพิ่งตกลง

เจ้าของงานทักว่าคำในแอป "มั่ว แปลดิบ ๆ เอามาใส่" ยกตัวอย่าง "สายงาน แบบ revision และสเกล"
แล้วให้ไปศึกษา `https://www.phayoo.com/ai-site-report-agent-proposal/details`

**สไตล์ที่ยืมมา** ทุกขั้นมีชื่อ **แล้วตามด้วยประโยคบอกว่าต้องทำอะไร** · ศัพท์ที่ช่างพูดทับศัพท์
อยู่แล้วไม่แปล (BOQ, price set, PDF, report, dashboard, progress, issue) · มีหัวข้อบอกตรง ๆ
ว่าระบบ **ไม่ทำ** อะไร แบบหัวข้อ Scope ของเขาที่เขียนว่า "ไม่ใช่ QS รับรอง BOQ หรือราคาทางวิชาชีพ"

**สิ่งที่ไม่ยืม** เขาใช้ "โปรเจค" เราใช้ **"โครงการ"** ตามเดิม เจ้าของงานเคาะเอง หลังผมเปิดต้นทาง
`km/Estimate1.pdf` เอกสารประกอบการสอนวิชาการประมาณราคาก่อสร้าง 1 วิทยาลัยเทคนิคเชียงใหม่
ซึ่งใช้ "โครงการ" "เจ้าของโครงการ" "ผู้ประมาณราคา" ตลอดเล่ม และตรงกับช่องบนแบบ ปร.
เว็บนั้นใช้ "โปรเจค" ได้เพราะขายบอท LINE ไม่ได้ออกแบบ ปร.4 ปร.5

## เจ็ด — เครื่องมือทดลอง วิธีเดิม ใช้ได้ผลอีกครั้ง

```
npx esbuild src/lib/region-fill.ts --bundle --format=iife --global-name=RF --outfile=public/zz-rf.js
```

โหลดในแท็บด้วย `<script src="/zz-rf.js">` แล้วดึง `ImageData` จากผืนวาดฐาน ซึ่ง**ตรงกับ
ชั้นวิเคราะห์ทุกพิกเซล** เพราะทั้งคู่ใช้ `fullPageScaleFor` กับ `planCropRender` เต็มหน้าเหมือนกัน
· เขียนภาพออกเป็น PNG ผ่าน `evaluate_script` + `filePath` แล้วถอด base64 ด้วย node
· **ลบ `public/zz-rf.js` ทุกครั้งหลังเสร็จ**

**ที่เพิ่งค้นพบ** MCP อัปโหลดไฟล์นอก workspace root ไม่ได้ ต้อง `cp` แบบเข้ามาในรีโปก่อน
แล้วลบทิ้ง · และการหาแนวเสาให้สแกนหาคอลัมน์หรือแถวที่มีหมึก > 35% **ในช่วงนอกตัวอาคาร**
จึงไม่ปนกับผนัง กระเบื้อง หรือสัญลักษณ์
