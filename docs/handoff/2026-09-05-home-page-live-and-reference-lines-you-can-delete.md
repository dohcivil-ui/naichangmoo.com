# 2026-09-05 — หน้าแรกใหม่ขึ้นจริงแล้ว และเส้นอ้างอิงที่ลบได้เป็นครั้งแรก

**สาขา** `initial-project/nextjs-foundation` · **รุ่น** 0.99.0 ยังไม่ปักธง **ยังไม่ push**
**ทำไมปิดเซสชัน** เซสชันยาวมาก ทำไปสี่คอมมิตและมีงานค้างห้าเรื่องที่เจ้าของงานสั่งระหว่างทาง

> ข้ามขั้น `/grill-with-docs` สำหรับงานสองข้อแรก เพราะไม้ต่อฉบับก่อนเคาะแนวทางไว้ครบแล้ว
> และเจ้าของงานตอบทางแยกที่เหลือสด ๆ ระหว่างทำ · **งานที่ค้างอยู่ในหัวข้อห้าต้อง grill ก่อน**

---

## หนึ่ง — เซสชันใหม่ทำอะไรก่อน เรียงตามลำดับ

1. `git log --oneline -6` ต้องเห็น `5eff930` บนสุด · `git status` เหลือแค่ `next-env.d.ts`
   กับ `.design/` `image/` ที่ค้างมานาน
2. **เช็คว่ามี session อื่นทำงานบนสาขาเดียวกันหรือเปล่า** ด้วย `ListAgents` — เซสชันนี้ชนกับ
   `naichangmoo-fa` จริงตอนเริ่ม เขาคอมมิต `821349a` และ `cacd8fe` ทับระหว่างที่ผมกำลังเขียนโค้ด
   เจ้าของงานสั่งให้เขาหยุดแล้วให้ผมทำต่อ · **`git log` ที่อ่านตอนเปิดเซสชันเก่าได้ภายในนาที**
3. dev server: `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/`
   · **หน้า markup ตอบ 500 "Jest worker encountered ... exceeding retry limit" คือ dev server
   ค้าง ไม่ใช่โค้ดพัง** เจอซ้ำอีกครั้งวันนี้ ยืนยันจากตัวตอบจริงแล้วว่าเป็นข้อความนั้นเป๊ะ
   ปิดโปรเซส node ของ next ทั้งหมดแล้วเปิดใหม่ หายทันที
   · **`Start-Process pnpm dev -WindowStyle Hidden` ไม่ทำงาน** เพราะ pnpm เป็น .cmd shim
   ใช้ `(pnpm dev > dev.log 2>&1 &)` ผ่าน Bash แทน แล้วรอด้วย `until grep -q "Ready in" dev.log`
4. อ่าน skill `drawing-geometry` ก่อนแตะเรื่องไล่ขอบทุกครั้ง

## สอง — สิ่งที่เสร็จวันนี้ สี่คอมมิต

| คอมมิต | เรื่อง |
|---|---|
| `5b0f8bc` | **หน้าแรก ESTIMETR เป็นแนว variant-b แล้ว** ตัวเลขอ่านจากฐานจริงทั้งหมด ลบต้นแบบทิ้ง |
| `5b175ff` | **คลิกเลือกเส้นระยะจริงกับแนวเสาได้ แล้วลบทีละเส้น** ซึ่งก่อนหน้านี้ทำไม่ได้เลย |
| `d758e1c` | **ล้างสเกลของหน้าได้** และแก้บั๊กเลือกเส้นค้างข้ามหน้า · `AGENTS.md` ข้อ 9 ใหม่ |
| `5eff930` | ถอดเครื่องมือ "เลื่อน" ออก เพราะซ้ำกับ "เลือก" ทุกอย่างยกเว้นการคลิกเลือก |

### ของใหม่ที่เกิดขึ้น

- `src/lib/estimeter-home-status.ts` — แปลงตัวเลขจากฐานเป็นคำบนป้ายสี่ขั้นและบรรทัด
  สิ่งที่ต้องทำต่อ · กติกาว่าขั้นไหนจบเขียนเป็นตารางในหัวไฟล์ · เทสต์ 11 ข้อ
- `src/lib/estimeter-home-assistant.ts` — บทของผู้ช่วย คำตอบยกจาก `method-claims` ทั้งดุ้น
  มีเทสต์เทียบตัวอักษร ห้ามพิมพ์ใหม่ให้สวยขึ้น
- `summarizeDrawingProgress()` และ `clearCalibration()` ใน `drawing-repository.ts`
- `hitTestSegments()` ใน `drawing-measurement.ts` — แยกจาก `hitTest` โดยตั้งใจ
- `EstimeterHomeBoard` และ `EstimeterHomeAssistant` ใน `components/estimeter/`
- `EntitlementStrip` ใน `entitlement-status.tsx` — แถบสิทธิ์บรรทัดเดียว กางดูครบใน `<details>`

## สาม — คำตัดสินของเจ้าของงานวันนี้ อย่าถามซ้ำ

| ทางแยก | คำตัดสิน |
|---|---|
| ผู้ช่วยบนหน้าแรกวางที่ไหน | **แผงผู้ช่วยกลาง** ไม่ใช่การ์ดในหน้า ตามกฎ IP-185 ที่ว่าทั้งเว็บมีช่องผู้ช่วยช่องเดียว |
| บรรทัด "อีก 31 หน้ายังไม่ได้ตั้ง" | **เขียนฟังก์ชันอ่านใหม่ให้เลขจริง** ไม่ตัดบรรทัดทิ้ง |
| บล็อกสิทธิ์การใช้งาน | **ย่อเป็นแถบบรรทัดเดียว** มีทางกดดูรายละเอียดเต็ม |
| ปุ่มลบเส้นอ้างอิงอยู่ที่ไหน | **แถบเส้นที่เลือก เหนือแถบสถานะ** ไม่ใช่ในแผงขวา ไม่เบียดผืนวาด |
| ล้างสเกลแล้วเส้นหายไปด้วยไหม | **กดได้เฉพาะเมื่อหน้านั้นไม่มีเส้นเหลือแล้ว** ไม่ลบเป็นผลข้างเคียง |
| ปุ่ม "เลื่อน" ที่ซ้ำกับ "เลือก" | **เอาออก** |
| ต่อผู้ช่วยเข้า AI provider | **เอาตามที่เสนอ ปลอดภัยไว้ก่อน** ดูหัวข้อห้าข้อห้า |
| ไล่หาบั๊กระหว่างทำงาน | **เป็นกฎถาวรแล้ว** `AGENTS.md` ข้อ 9 |

## สี่ — บั๊กที่เจอและแก้ไปแล้ว สามตัว

1. **ปุ่มลอยของแผงผู้ช่วยตัดคำเป็นสามบรรทัด** สูง 64px แทน 50px เพราะ `.assistant-dock`
   ไม่ได้ตั้งความกว้าง พอย่อแผงแล้วเหลือ 0 ปุ่มข้างในจึงถูกบีบ · ใส่ `white-space: nowrap`
   วัดหลังแก้ได้ 180×50 · **เป็นบั๊กของแพลตฟอร์ม มีผลกับทุกแอปที่เสียบผู้ช่วย**
2. **เส้นอ้างอิงที่เลือกไว้ค้างข้ามหน้า** เลือกบนหน้า 1 เลื่อนไปหน้า 2 แถบยังบอกว่าเลือกอยู่
   กด Delete แล้วลบของบนหน้าโน้นจริงโดยไม่มีอะไรบนจอเปลี่ยน · ทุกทางที่เปลี่ยนหน้า
   ผ่าน `goToPage()` ตัวเดียวแล้ว
3. **สเกลตั้งได้แต่เอาออกไม่ได้** ทำให้เซสชันนี้ตั้งสเกลทดสอบค้างไว้บนแบบจริงของเจ้าของงาน
   แล้วเอาออกเองไม่ได้ · แก้ด้วย `clearCalibration()` แล้วใช้ปุ่มนั้นล้างของตัวเองออก

## ห้า — งานถัดไป เรียงตามที่เจ้าของงานสั่ง

1. **ผู้ช่วยพาทัวร์ในหน้าแบบ** เจ้าของงานเลือกเป็นงานถัดไป · เขาถามว่า "บังคับตั้งสเกลก่อน
   ใช่มั้ย ถ้าใช่ถือว่าดีมาก **แต่ควรมี tips แนะนำ และชี้ไปยังปุ่มตั้ง scale ด้วย เหมือน
   ผู้ช่วยพาทัวร์**" · ยืนยันแล้วว่าบังคับจริง `toolNeedsScale` ล็อกห้าตัวที่คืนค่าเป็นเมตร
   ส่วนนับจำนวน ร่างกริด ระยะจริง เปิดไว้ถูกแล้ว **แต่มันล็อกเงียบ ๆ ไม่บอกว่าทำไม**
   · เสียบผ่าน `<AppAssistant>` ได้เลย เพราะ `AppShell` โหมด workspace มี `AssistantDockHost`
   · ขั้นของทัวร์ควรคิดจากสถานะจริง ไม่ใช่เขียนตาย และควรมีตัวชี้ไปที่ปุ่มเป้าหมาย
   เช่น `data-coach-active` แล้ว CSS วงแหวนกะพริบ ซึ่งต้องนิ่งใต้ `prefers-reduced-motion`
2. **หน้าโครงการยังเป็นภาษาเดิม** เจ้าของงานเปิด `/apps/estimeter/projects/<id>` แล้วถามว่า
   "ทำไมยังไม่เปลี่ยน" · ผมตรวจแล้วเจอสี่ปัญหาจริง **การ์ดสี่ขั้นแตกแถว ขั้น 4 ตกลงไปอยู่
   คนเดียว** · ลำดับงานสี่ขั้นซ้ำกับหน้าแรกคนละหน้าตา · การ์ดบัญชีราคา ประมาณราคา และ BOQ
   เป็นการ์ดขาวสูงเกือบเต็มจอที่มีข้อความสองบรรทัด · พาดหัวชื่อโครงการใหญ่กว่าชื่อแอป
   · คลาส `.eh-` ใช้ซ้ำได้ทันที
3. **ดูดจุดเข้าเส้นจริงในแบบ** เจ้าของงานเสนอให้ดูดเข้าจุดปลายและจุดตัดของเส้นในแบบจริง
   ไม่ใช่แค่เส้นที่เราวาด · **ข้อเสนอของผมคืออ่านเส้นเวกเตอร์จาก PDF ตรง ๆ ด้วย
   `getOperatorList()` ของ pdf.js ไม่ใช่ไล่หาจุดจากพิกเซล** เพราะแบบจาก CAD เป็นเวกเตอร์อยู่แล้ว
   ได้พิกัดปลายเป๊ะ เร็วกว่า และไม่ขยับตามซูมโดยธรรมชาติ · แบบสแกนค่อยถอยไปใช้พิกเซล
   · ยังต้องกรองว่าเส้นไหนคือผนังคือเสาอยู่ดี ตามบทเรียนข้อแปดของ skill **ต้อง grill ก่อน**
4. **หมุนแบบให้ตรง** เจ้าของงานเสนอให้หมุนทีละองศา · **ข้อเสนอของผมคือทำกลับด้าน
   ให้ "ล็อกแนวเส้น" ล็อกกับทิศของแนวเสาที่ผู้ใช้ร่างไว้ แทนแนวนอนแนวตั้งของจอ**
   แบบเอียงก็ไม่ต้องหมุนเลย · และไม่ต้อง resample ภาพ ซึ่งสำคัญเพราะวาดอยู่ที่
   0.53 พิกเซลต่อพิกเซลจอ ไม่มีระยะเผื่อให้เสีย · ถ้าจะหมุนจริงก็ไม่ควรให้คนกดหาองศาเอง
   ให้ทำฮิสโทแกรมทิศของเส้นยาวแล้วเสนอครั้งเดียวว่า "แบบเอียง 0.7° จัดให้ตรงไหม"
   **ต้อง grill ก่อน**
5. **ต่อผู้ช่วยเข้า AI provider** เจ้าของงานถามว่าควรต่อไหม แยกส่วนจ่ายเงิน ใช้ของจีน
   · **เขาเคาะแล้วว่าเอาตามที่ผมเสนอ "ปลอดภัยไว้ก่อน เราต้องเน้นเรื่องความปลอดภัยของลูกค้า"**
   ข้อตกลงคือ ผูกผู้ให้บริการไว้หลังหน้ากากเดียว อย่าแต่งงานกับเจ้าไหน · เพดานเงินต่อองค์กร
   ตั้งที่ฐาน ไม่ใช่ที่หน้าจอ · หมดเพดานแล้วตกกลับมาเป็นปุ่มสคริปต์ ไม่ใช่ขึ้นข้อความแดง
   · **ห้ามส่งชื่อโครงการและเนื้อแบบออกไปนอกระบบ** ส่งได้เฉพาะคำถามกับบทตั้งต้นที่เขียนตายไว้
   เพราะมีบุคลากรกรมทางหลวงใช้ และผู้ให้บริการหลายเจ้าเอาข้อมูลไปเทรนต่อโดยปริยาย
   · เส้นที่ห้ามข้ามคือ `method-claims` สองข้อ "ถามซ้ำกี่ครั้งก็ได้คำตอบเดิม" และ
   "ตัวเลขเงินไม่เคยมาจากปัญญาประดิษฐ์" · **ควรเป็น ADR · ต้อง grill ก่อน**
6. **เรื่องที่ยังค้างจากไม้ต่อฉบับก่อน** ด้านลึกยังไม่มีเลขที่แบบเขียนเมื่อวางเส้นระยะแกนเดียว
   · ค่าเผื่อสำหรับห้องเล็ก ยังไม่ได้ถามเจ้าของงาน · เก็บไฟล์แบบไว้ในระบบ แตะ R2 กับ security
   · IP-187 · IP-230 ห้ามเริ่ม

## หก — ของที่ตรวจแล้วในเบราว์เซอร์ จดไว้กันทำซ้ำ

- **หน้าแรกใหม่ตรวจครบทุกความกว้าง** 1440 · 1024 · 768 · 390 ไม่มีเลื่อนซ้ายขวาสักความกว้าง
  · การ์ดสี่ขั้น 4 คอลัมน์ → 2 ที่ 1024 และ 768 → 1 ที่ 390
  · **พับด้วย container query ไม่ใช่ media query** เพราะแผงผู้ช่วยกินความกว้าง 396px
- **คอนทราสต์ผ่านหมด** วัดด้วย `getComputedStyle` ไล่พื้นโปร่งขึ้นไปหาพื้นทึบ
  เดสก์ท็อป 73 คู่ มือถือ 71 คู่ ผ่าน 4.5:1 ทั้งหมด ต่ำสุด 4.59
- **ฟอนต์ตัวเลขเป็นของจริง** IBM Plex Sans Thai เลข 0-9 กว้าง 6.72px เท่ากันหมด ต่างกัน 0.00%
- **การลบเส้นอ้างอิงลงฐานจริง** โหลดหน้าใหม่แล้วเปิดแบบใบเดิม เส้นที่ลบไม่กลับมา
- **การล้างสเกลลงฐานจริง** ทั้ง 32 หน้าของแบบทดสอบกลับมาเป็น "ยังไม่ตั้งสเกล"
- **เทสต์ integration รันจริงกับฐานได้** `$env:ESTIMETR_DB_TESTS = "1"` แล้ว vitest ไฟล์นั้น
  ผ่าน 16 ข้อ · มันสร้าง fixture ของตัวเองและล้างทิ้งใน afterAll
- **MCP คลิกไม่เหลื่อม** สั่ง (535,268) ลงจริง (534.99,267.99) ยืนยันเลข 0 อีกครั้ง
  · **skill `drawing-geometry` ยังจดว่าเหลื่อม 46 พิกเซล ควรแก้**
- **วิธีวาดบนผืนวาดด้วยสคริปต์** ใช้ `dispatchEvent(new PointerEvent('pointerdown'))` ได้
  สำหรับเครื่องมือร่างกริดและระยะจริง เพราะไม่เรียก `setPointerCapture`
  **แต่ต้องหน่วงระหว่างสองคลิก** ไม่งั้นคลิกที่สองใช้ handler เก่าที่ยังไม่เห็น state ของคลิกแรก
  · เครื่องมือเลือกทำแบบนั้นไม่ได้ ต้องใช้กล่องเล็ง `#aim-box` แบบ `pointer-events:none`
  แล้วให้ MCP คลิกที่ uid ของมัน
- **`.tmp-verify-drawing.pdf` ไม่อยู่ใน `.gitignore`** ต้องลบทุกครั้งก่อน commit · `*.log` อยู่แล้ว

## เจ็ด — คำสั่งเปิดเซสชันใหม่

เปิด session ใหม่ในโฟลเดอร์ `d:\AIProject\naichangmoo` แล้ววางข้อความนี้

```
อ่าน AGENTS.md แล้ว docs/handoff/2026-09-05-home-page-live-and-reference-lines-you-can-delete.md
แล้วทำหัวข้อหนึ่งตามลำดับ เริ่มจากตรวจ git ตรวจว่ามี session อื่นบนสาขาเดียวกันไหม แล้วตรวจ dev server

งานหลักคือหัวข้อห้าข้อหนึ่ง ผู้ช่วยพาทัวร์ในหน้าแบบ
เปิดหน้าแบบดูด้วยตาก่อน แล้วบอกผมว่าจะทำอะไรบ้างก่อนลงมือ
ตอนนี้เครื่องมือวัดถูกล็อกจนกว่าจะตั้งสเกล ซึ่งถูกแล้ว แต่มันล็อกเงียบ ๆ ไม่บอกว่าทำไม
และไม่ชี้ว่าต้องกดตรงไหนก่อน

เสร็จแล้วต่อด้วยข้อสอง หน้าโครงการยังเป็นภาษาเดิม ให้เข้าชุดกับหน้าแรกใหม่

ข้อบังคับ
- แถบบนกับท้ายเว็บเป็นของ AppShell ห้ามวาดซ้ำ และห้ามให้หัวเรื่องซ้อนกันสองชั้น
- สีใช้ตัวแปรธีมเท่านั้น · ตัวหนังสือกับพื้นวัดด้วย getComputedStyle ต้องผ่าน 4.5:1 ทุกคู่
- ตัวเลขใช้ฟอนต์ตัวเลขของระบบ ไม่ใช่ Prompt
- ตรวจทุกความกว้างจริง มือถือ แท็บเล็ต เดสก์ท็อป

อ่าน skill drawing-geometry ก่อนแตะเรื่องไล่ขอบทุกครั้ง
ไล่หาบั๊กในโค้ดที่แตะแล้วรายงานด้วย ตาม AGENTS.md ข้อ 9
ตัวเลขทุกตัวต้องมาจากเส้นบอกระยะหรือสูตรที่ตรวจซ้ำได้ ห้ามเดา ห้ามมั่ว
ทดลอง ตรวจสอบจนผ่าน ค่อยส่งมานำเสนอ · ห้าม push ห้าม pnpm release จนกว่าผมจะสั่ง
ถ้า context เหลือประมาณ 15% ให้หยุด ทำ handoff เพื่อย้ายไป session ใหม่
```
